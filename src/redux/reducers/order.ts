/* eslint-disable @typescript-eslint/explicit-module-boundary-types */
/* eslint-disable import/extensions */

export const order = (state = {}, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
